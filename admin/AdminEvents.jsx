import EventManager from '../../components/shared/EventManager';

// clubId undefined => admin creates/sees platform-wide + all club events
const AdminEvents = () => <EventManager />;

export default AdminEvents;
